>include "example_source/tetris/board.f12";

fun main {
    init_board();

    clear_screen();

    while true {
        print_board();
        process_frame();
        sleep(FRAME_DURATION_MS);
    }
}