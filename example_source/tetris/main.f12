>include "example_source/tetris/config.f12";
>include "example_source/tetris/board.f12";

fun main {
    init_board();

    clear_screen();

    while true {
        print_board();
        process_frame();
        sleep(300);
    }
}