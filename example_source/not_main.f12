unit Toy {
    int size;
}

unit Child {
    string name;
    Toy toy;
}

unit Human {
    string name;
    int age;
    string[] addresses;
    Child[] children;
}

fun add(float[] x, float y) => A {
    const Human human = Human|  |;
    human.children[0].name = "labas";
    human = Human|  |;
}