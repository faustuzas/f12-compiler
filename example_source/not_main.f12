unit Toy {
    int size;
}

unit Child {
    string name;
    Toy toy;
}

unit Human {
    string name;
    int age;
    string[] addresses;
    Child[] children;
}

fun add(float[] x, int y) => A {
    const Human human = Human| name: "Faustas", age: 5, addresses: [ "Sauletekio 6" ], children: [ Child| name: "aaa", toy: Toy | size: 5 | | ]|;
    human.children[0].name = "labas";
}

int c;
fun main => int {
    int b;
    Toy[] t;
}