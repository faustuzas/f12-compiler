unit Toy {
    int size;
}

unit Child {
    string name;
    Toy toy;
}

unit Human {
    string name;
    int age;
    string[] addresses;
    Child[] children;
}

fun add(float[] x, float y) => A {
    // Human human = Human|  |;
    // human.children[0].name = "labas";
    // human.children.name = "labas";

    Human[] a = [1, 2];
}